<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ben.android.demo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name=".MainActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- UI -->
        <activity android:name=".UI.UIDemoMain" >
        </activity>
        <activity android:name=".UI.UIByCodeActivity" >
        </activity>
        <!-- Surface -->
        <activity android:name=".SurfaceView.SurfaceViewDemoMainActivity" >
        </activity>
        <activity android:name=".SurfaceView.LittleBallActivity" >
        </activity>
        <!-- Threading -->
        <activity android:name=".Threading.ThreadingMainActivity" >
        </activity>
        <activity android:name=".Threading.ThreadJoinActivity" >
        </activity>

        <!-- Storage -->
        <activity android:name=".storage.StorageMainActivity" >
        </activity>
    </application>

</manifest>